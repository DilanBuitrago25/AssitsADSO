@model IEnumerable<ClaseDatos.Usuario>


<br />
<div class="container-fluid">
    <div class="page-header">
        <div class="row">
            <div class="col-sm-6">
                <h3>Aprendices asociados a la Ficha: @ViewBag.CodigoFicha</h3>
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="@Url.Action("Index","Instructor")">Home</a></li>
                    <li class="breadcrumb-item">Ficha</li>
                    <li class="breadcrumb-item">@ViewBag.CodigoFicha</li>
                    <li class="breadcrumb-item active">Consultar Aprendices</li>
                </ol>
            </div>
            <div class="col-sm-6">

            </div>
        </div>
    </div>
</div>
<div class="col-sm-12">
    <div class="card">
            <div class="card-header d-flex align-items-center">
                <div class="col-sm-4">
                    <h5>Tabla de datos - Aprendices</h5>
                    <br />
                    @using (Html.BeginForm("Index", "Fichas", FormMethod.Get))
                    {
                        <div class="d-flex align-items-center">
                            <label for="estadoFiltro" class="me-2">Estado:</label>
                            <select class="form-select me-2" id="estadoFiltro" name="estadoFiltro">
                                <option value="" @(ViewBag.EstadoFiltro == "" ? "selected" : "")>Todos</option>
                                <option value="true" @(ViewBag.EstadoFiltro == "true" ? "selected" : "")>Activo</option>
                                <option value="false" @(ViewBag.EstadoFiltro == "false" ? "selected" : "")>No Activo</option>
                            </select>
                            <input class="btn btn-success btn-sm" type="submit" value="Filtrar" />
                        </div>
                    }
                </div>
                <div class="col-sm-8 d-flex justify-content-end">
                    @if (@Session["TipoUsuario"].ToString() == "InstructorAdmin")
                    {
                        <button class="btn btn-primary" onclick="window.location.href='@Url.Action("Asociar_Aprendiz_Ficha", "Fichas", new { id = ViewBag.FichaId })'">
                            Agregar Aprendiz
                        </button>
                    }
                </div>
        </div>

        <div class="card-body">
            <button type="button" class="btn btn-primary" onclick="window.location.href='@Url.Action("GenerarReportePDFAprendiz", "Fichas")'" id="BottomPdf">
                Generar Reporte PDF
            </button>
            <div class="table-responsive">
                <table class="display datatables" id="ajax-data-object">
                    <thead>

                        <tr>
                            <th>Id</th>
                            <th>Documento</th>
                            <th>Nombre</th>
                            <th>Correo</th>
                        </tr>

                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.Id_usuario)</td>
                                <td>
                                    @Html.DisplayFor(model => item.Tipo_Documento_usuario)
                                    @Html.DisplayFor(model => item.Documento_usuario)
                                </td>
                                <td>
                                    @Html.DisplayFor(model => item.Nombre_usuario)
                                    @Html.DisplayFor(model => item.Apellido_usuario)
                                </td>
                                <td>@Html.DisplayFor(model => item.Correo_usuario)</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>Id</th>
                            <th>Documento</th>
                            <th>Nombre</th>
                            <th>Correo</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="col-sm-12 d-flex justify-content-end">
                <button class="btn btn-light" type="button" style="margin-top: 15px;"><a href="@Url.Action("Index","Fichas")">Volver</a></button>
            </div>
        </div>
    </div>
</div>


@*<h2>Aprendices de la Ficha @ViewBag.FichaId</h2>
@foreach (var item in Model)
{
    <table class="table">
        <td>
            @Html.DisplayFor(modelItem => item.Id_usuario)
        </td>
        <td>
            @Html.DisplayFor(model => item.Tipo_Documento_usuario)
            @Html.DisplayFor(model => item.Documento_usuario)
        </td>
        <td>
            @Html.DisplayFor(model => item.Nombre_usuario)
            @Html.DisplayFor(model => item.Apellido_usuario)
        </td>
        <td>
            @Html.DisplayFor(model => item.Correo_usuario)
        </td>
    </table>
}
@if (@Session["TipoUsuario"].ToString() == "InstructorAdmin")
{
    <button class="btn btn-primary" onclick="window.location.href='@Url.Action("Asociar_Aprendiz_Ficha", "Fichas", new { idFicha = ViewBag.FichaId })'">
        Agregar Aprendiz
    </button>
}*@
